# B-Square 추천 시스템 개발 및 오류 일지

본 문서는 추천 클래스 폴더 및 인기 클래스 관리 시스템 구축 과정에서 발생한 주요 오류와 해결 과정, 그리고 현재의 한계점을 기록합니다.

## 1. 주요 수정 사항 요약
- **데이터 격리 (Data Isolation)**: 인기 클래스와 추천 폴더 저장 로직을 분리하여, 한 쪽을 저장할 시 다른 쪽 데이터가 삭제되던 현상 해결.
- **안정적 타겟팅 (Robust Targeting)**: 폴더 추가/수정 시 DOM의 ID가 변하더라도 **ID -> 시각적 강조 -> 폴더 제목 검색** 순으로 추적하는 3중 방어막 구축.
- **상태 보호 (State Protection)**: 클래스 선택 모달이 열려 있는 동안은 데이터가 새로고침되어 작업 내용이 날아가는 것을 방지.
- **재귀 오류 해결**: 전역 함수 노출 시 발생하던 스택 오버플로(Stack Overflow) 오류 수정.

## 2. 발생했던 주요 오류 및 대처

### [오류 1] 타겟 폴더를 찾을 수 없음 (Target Not Found)
- **증상**: 클래스 추가 버튼 클릭 시 "대상을 찾을 수 없습니다" 알림 발생.
- **원인**: 탭 전환이나 자동 새로고침 시 기존 폴더 엘리먼트가 삭제되고 새 ID를 가진 엘리먼트가 생성됨.
- **해결**: 대상 폴더의 "제목"을 기반으로 화면 전체를 검색하는 휴리스틱 로직(`Ultimate Bulletproof Matching`) 도입.

### [오류 2] 인기 섹션 추가 불가
- **증상**: 인기 클래스 섹션에 클래스가 추가되지 않음.
- **원인**: 인기 섹션은 '폴더' 형태가 아니었으나, 검색 함수가 `.recommend-folder-item` 클래스만 찾으려 했음.
- **해결**: `popular_folder`라는 특수 ID를 명시적으로 처리하도록 `openClassSearch` 함수 개편.

### [오류 3] 데이터 유실 (Nuclear Delete)
- **증상**: 추천 폴더를 저장하면 인기 클래스가 사라지는 현상.
- **원인**: 서버 API에서 `DELETE FROM recommendations` 명령 시 조건절(`WHERE type = ?`)이 누락되어 전체 테이블이 삭제됨.
- **해결**: API 로직을 `type`별로 삭제 및 삽입하도록 전면 수정.

## 3. 남아있는 도전 과제 (Challenge)
- **비동기 렌더링 타이밍**: 브라우저의 비동기 처리 특성상, 입력창의 텍스트가 DOM에 반영되는 찰나의 순간에 검색이 이뤄지면 "제목" 매칭이 실패할 가능성이 희박하게 존재함 (마지막 스크린샷 사례).
- **브라우저 캐시**: 스크립트 수정 후에도 브라우저가 옛날 버전의 JS 파일을 기억하고 있어 수정 사항이 반영되지 않는 이슈가 반영되지 않는 이슈가 반복됨. (버전 파라미터 `?v=2010` 등을 통해 강제 갱신 중)

---
**기록일**: 2026-03-21
**최종 버전**: Ver 2026.03.21-20:10
