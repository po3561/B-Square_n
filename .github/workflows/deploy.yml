name: Disabled GitHub Pages Deploy (Cloudflare Pages is primary)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub Pages deploy disabled. Use Cloudflare Pages + Functions + D1."
